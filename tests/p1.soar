package require TinySoar;

sp {propose*wait
    (state <s> ^superstate nil)
    -->
    (<s> ^operator <o> +)
    (<o> ^name wait)}

sp {reconsider*wait
    (state <s> ^operator <o>)
    (<o> ^name wait)
    -->
    (<s> ^operator <o> @)}
